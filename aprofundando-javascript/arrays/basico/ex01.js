// Crie um array chamado frutas com 5 frutas.
/** Exiba o array completo.
    Exiba apenas a primeira fruta.
    Exiba apenas a última fruta.
    Exiba o tamanho do array. 
*/

const frutas = ['Maça', 'Bana','Uva', 'Laranja', 'Manga']

// exibir array completo:
console.log(frutas);

// exibir apenas a primeira fruta
console.log(frutas[0]);

// exibir apenas a última fruta
console.log(frutas[frutas.length - 1]);

// tamanho do array
console.log(frutas.length);