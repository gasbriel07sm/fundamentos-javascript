// CONSTANTES -> são variáveis que não podem ter seu valor alterado
// Não podemos criar constantes com palavras reservadas
// Constantes precisam ter nomes significativos
// Não pode começar o nome de uma constante com um número
// Não podem conter espaços ou traços
// camelCase = padrão (convençaão) da linguagem 
// Case-sensitive (maiuscula <> minuscula)
// Não podemos modificar o valor de uma constante
// Uma constante precisa ser inicializada ao ser declarada obrigatoriamente

const nome = 'Gabriel';
console.log(nome);


// OPERAÇÕES

// + - * /

const primeiroNumero = 5;
const segundoNumero = 10;
const conta = primeiroNumero * segundoNumero;
const resultadoDuplicado = conta * 2;
let resultadoTriplicado = resultado * 3;
resultadoTriplicado += 5;
console.log(conta);
console.log(resultadoDuplicado);
console.log(resultadoTriplicado);

console.log(typeof primeiroNumero);

